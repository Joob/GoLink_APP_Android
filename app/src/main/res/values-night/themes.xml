<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.VueFileManager" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
    </style>

    <!--
        No night override for Theme.VueFileManager.Splash: the single splash
        style in values/themes.xml already adapts via @color/splash_background
        (the white circle icon stays identical to the launcher in both modes).
    -->
</resources>
